{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: day9",
            "request": "launch",
            "command": "npm run day9",
            "cwd": "${workspaceFolder}"
        }
    ]
}