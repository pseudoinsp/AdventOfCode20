{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: day5",
            "request": "launch",
            "command": "npm run day5",
            "cwd": "${workspaceFolder}"
        }
    ]
}