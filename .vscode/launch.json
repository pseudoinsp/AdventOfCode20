{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: day8",
            "request": "launch",
            "command": "npm run day8",
            "cwd": "${workspaceFolder}"
        }
    ]
}