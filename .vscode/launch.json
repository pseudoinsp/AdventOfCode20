{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Run Script: day7",
            "request": "launch",
            "command": "npm run day7",
            "cwd": "${workspaceFolder}"
        }
    ]
}